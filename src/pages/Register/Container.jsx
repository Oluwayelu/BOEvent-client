import { Auth } from 'components/layout';

import RegisterView from './View';

export const RegisterContainer = () => {
  return (
    <Auth title="Register">
      <RegisterView />
    </Auth>
  );
};
